package Scripts;

import io.appium.java_client.TouchAction;
import io.appium.java_client.touch.LongPressOptions;
import org.openqa.selenium.WebElement;

public class SwipeAction extends TestBase{


    TouchAction t =new TouchAction(driver);
//    WebElement first=driver.findElementByXPath();
//    WebElement second=driver.findElementByXPath();
    //t.longPress(long)
         //   t.longPress(LongPressOptions.longPressOptions().withElement(element(first)).withDuration(ofSeconds(2))).moveTo(element(second)).release().perform();

            //long press (source)/move to (destination)//release
          //  WebElement source=driver.findElementByXPath().get(0);
//    WebElement dest=driver.findElementByXPath().get(1);
  //  t.longPress(element(source)).moveTo(element(dest)).release().perform();


}
